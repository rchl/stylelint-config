# stylelint-config-rchl

## Installation

```sh
$ npm i -D stylelint-config-rchl stylelint
```

## Usage

Add in package.json:

```json
  "stylelint": {
    "extends": [
      "stylelint-config-rchl"
    ]
  }
```
